source 'https://rubygems.org'

# Only needed to run this plugin's test suite. ArchivesSpace supplies these at
# runtime, so nothing here is installed into a deployed instance.
group :test do
  gem 'nokogiri', '>= 1.19.3'
  gem 'rspec', '~> 3.12'
end
